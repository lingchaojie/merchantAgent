# 商家智能体平台 — 实现进度

> 更新日期：2026-07-12 ｜ 阶段：**Phase 1 进行中（M1–M6 + desktop-local tool 竖切完成）** ｜ 设计见 `docs/phase1-设计.md` ｜ 仍全本地/reference

本文件记录**代码实现**的真实进度；架构与调研见 `research/`（`00` 主报告、`10` 企微多租户方案、`11` Phase 0 权限设计）。Phase 1 里程碑进度见第 "Phase 1 进度" 节。

---

## 1. 一句话现状

Phase 0 骨架已升级为**真正的产品形态**：真 LLM 编排 + Skill 渐进披露 + OpenFGA + SSE 桌面桥 + 签名 capability 本地执行。2026-07-12 的 Windows 竖切已在 unpacked App 上完成 sales 60% read/写拒绝、planner 原生确认写至 80%/version 2、sales read-back、审计链和实时撤权；确定性测试同时覆盖幂等、取消、冲突、签名/版本拒绝和 deny/no-local-request。仍是**全本地/reference**：参考 SQLite 证明执行拓扑，不代表真实客户 ERP/数据库已接入；身份仍为可自选的 mock userId。

---

## 2. 已确定的产品决策（约束）

| # | 决策 |
|---|---|
| 部署 | SaaS 多租户 |
| 首批行业 | 贸易 + 制造 |
| 身份源 | 企业微信（**仅鉴权 + 复用组织架构**，非中心渠道；免全量导入员工）|
| 自研/开源 | 核心自研 + 开源组件；系统无关（无 API 就把 DB 操作封装成 skill/MCP）|
| 商业模式 | 服务商生态 + 按席位（体外合同，不依赖企微计费）|
| 合规 | 无硬要求 |
| 客户端 | **桌面 app 为主**（Electron，对标 Cursor/Codex）|

---

## 3. 仓库结构

```
merchantAgent/
├─ backend/      Go 后端（编排/授权/连接器/审计 + MCP server + HTTP API）
├─ desktop/      Electron + Vite + React + TS 桌面壳
├─ research/     调研与设计文档（00–11 + 补充）
└─ docs/         本进度文档
```
> git 已初始化，远端 `origin`（GitHub），主干 `main`。

## 4. 后端（Go）— 已实现

module `github.com/merchantagent/backend`，go 1.25（openfga/language 转换器抬高的）。

| 包 | 内容 | 状态 |
|---|---|---|
| `org/` | IdP 适配器接缝：`Adapter` 接口 + `OrgSnapshot/User/Dept/Tag/Principal` 类型 + `MockAdapter`（读 YAML） | ✅ 单测绿 |
| `sync/` | 纯逻辑：职位文本→角色映射(fail-closed)、`OrgSnapshot→OpenFGA 元组`、幂等 diff 对账 | ✅ hermetic 单测绿 |
| `authz/` | `model.fga` 授权模型 DSL + OpenFGA SDK 封装(`Check`/`ListObjects`/`ApplyDiff`) + `Syncer`(Seed) | ✅ 6 验收用例绿 |
| `connector/` | 企业系统抽象：`Tool/Connector` 接口 + `ToolSpec`(带授权声明) | ✅ |
| `connector/mockerp/` | mock ERP：`query_order_status` / `query_order_financials`(cost 域) / `check_material_kitting` | ✅ 单测绿 |
| `connector/clientexec/` | desktop proxy：签名包 ID/version/digest、角色/设备/幂等/确认契约 | ✅ 竖切绿 |
| `runtime/` | 确定性 Agent 循环：`KeywordRouter` + guard(§6.1 双查) + 哈希链审计 | ✅ hermetic 单测绿 |
| `e2e/` | 组合根测试：真 OpenFGA + mock ERP + runtime 复现"同问不同权" | ✅ 绿 |
| `cmd/mock-erp-mcp/` | 用官方 `modelcontextprotocol/go-sdk` v1.6.1 把 mock ERP 暴露成 stdio MCP server | ✅ 冒烟测试绿 |
| `cmd/agentd/` | HTTP API：`/login` `/chat`(SSE) `/audit` + local-tool result bridge | ✅ 确定性 + Windows 实测绿 |

**权限模型核心（`authz/model.fga`）**：
- `tenant`（=企微 corpid，一切按它命名空间隔离）、`department`（层级，`manager = leader or manager from parent` 向下继承；`member` 不继承防越权）、`role`、`data_domain`（桥接"能力授权"与"数据授权"）、`agent`、`tool`、`order`。
- 运行时铁律：**工具调用 = 记录级 viewer ∧ 数据域 viewer 取交集** → 结构上保证 **agent 权限 ≤ 用户权限**。

**已验证的行为**（真实 OpenFGA + agentd curl）：
- 销售问订单进度 ✅；问利润 ⛔（cost 数据域过滤）
- 销售经理/总经理问利润 ✅（manager 向下继承）
- 计划员问销售部订单 ⛔（`ListObjects` 预过滤为空）
- 全程哈希链审计、可验证

**运行**：
```bash
cd backend && docker compose up -d              # OpenFGA :18080
go test ./...                                    # 7 包全绿（e2e 连不上 OpenFGA 会自动 skip）
OPENFGA_API_URL=http://localhost:18080 go run ./cmd/agentd   # HTTP API :8765
```

## 5. 桌面壳（Electron + Vite + React + TS）— 已实现

对标 Cursor/Codex 架构：**Electron UI 壳 + 编译型后端(Go agentd)干重活**。栈：electron-vite + React 18 + TypeScript(严格) + Vitest。

**结构与安全**：
- `src/shared/contract.ts`：★ IPC 契约唯一真源（`AgentAPI/Answer/Principal/Channels`），main/preload/renderer 三端共享，形状漂移即编译报错。
- `src/main/`：`index.ts`(安全壳：contextIsolation + sandbox + nodeIntegration:false + CSP 仅连 loopback + 拦截外链)、`ipc.ts`(唯一特权桥)、`fsguard.ts`(路径沙箱：防 `../` 逃逸、覆盖写需确认)、`agentd.ts`(连/spawn Go 后端)。
- `src/main/local-tools/`：签名 capability 校验 + executor + `better-sqlite3` 参考企业 store；low_write 原生确认、幂等、乐观冲突和写后校验。
- `src/preload/index.ts`：contextBridge 暴露类型化 `window.agent`（渲染层零 Node）。
- `src/renderer/`：React UI。

**UI（Linear 风格，已实现）**：
- 设计系统 `theme.css`：深色近黑 `#0b0c0e`、靛紫强调 `#5e6ad2`、发丝边框、Inter、圆角 tokens。
- 组件：`Sidebar`(工作区头 + 会话列表 + 底部身份 + mock 标识)、`TopBar`(标题 + 身份 chip + Cmd+K)、`CommandPalette`(Cmd+K 键盘可导航：切身份/新建会话)、`ChatView`(消息行 + 头像 + **工具徽章** + **权限拦截锁态** + pending 打字动画)、`ResultCard`(按工具渲染结构化卡片：进度/财务/齐套欠料)、`EmptyState`(示例 prompt 芯片)、`Composer`(自增长 textarea，Enter 发送)。

**已验证**（typecheck + build + Windows 程序化核验）：
- `npm run typecheck` 双端 0 错误；`npm run build` electron-vite 三端打包成功（41 模块）；`vitest` fsguard 5 用例绿。
- Playwright 程序化确认：Linear tokens 全部生效（布局 248px 侧栏、`#0b0c0e` 背景、`#5e6ad2` 强调、Inter）；点示例→出消息+结构化卡片；销售问利润→**权限拦截锁态**；切总经理问利润→**同一问题变为通过 + 财务卡片(利润绿色)**；Cmd+K 面板正常。
- Electron 在本 WSL 用 `--disable-gpu --no-sandbox`（WSL workaround）干净启动、main 跑到位、零渲染层报错。
- Windows `dist:dir` 已验证 capability/公钥 hash 与源码一致，Electron 33.4.11 能加载打包后的 `better_sqlite3.node`；Vitest 102/102，1000×720 与 390×844 真窗口无水平溢出或控件重叠。

**运行**：
```bash
cd desktop && npm install
npm test        # vitest 安全用例
npm run build   # electron-vite 三端打包
npm run dev     # 开发（WSL 加 -- --disable-gpu --no-sandbox）
```

## 6. 真实 vs Mock —— 务必看清

| 能力 | 现状 | 说明 |
|---|---|---|
| 权限**执行**（OpenFGA 判断能否看成本）| ✅ **真实、正确** | 后端走真 OpenFGA `Check`；e2e + curl 已证 |
| 身份**断言**（"我是谁"）| ⚠️ **假的（自助选择）** | 从下拉选 userId，被直接信任；无密码/OAuth/会话。**任何人现在都能切成老板** |
| 订单/BOM 数据 | ⚠️ mock | `testdata/mock-erp.yaml`，非真实 ERP |
| 组织架构 | ⚠️ mock | `testdata/mock-org.yaml`，非真实企微通讯录 |
| 桌面浏览器预览（Playwright 看的那版）| ⚠️ **纯 mock** | 普通浏览器无 `window.agent` → 回退到 `agent.ts` 里写死的数据+规则，**不经后端/OpenFGA** |
| Electron → agentd → OpenFGA → 本地参考库 **整条链** | ✅ **Windows 实机已跑通** | 真 gpt-5.5 + unpacked App；仍使用 mock 身份和 reference 数据 |

**结论**：架构是"连真后端"的，权限执行是真的；但**当前所有演示跑在 mock 身份 + mock 数据上**。

## 7. 尚未实现（Gap）

- **企微真鉴权**：`WeComAdapter` 只有接口，未实现；OAuth 扫码登录未接（需公网 HTTPS + ICP 备案域名，用户暂无 → 已跳过）。
- **真实连接器**：无真实 ERP/DB/UI 自动化连接器；`ToolSpec` 授权声明框架已就位，等接真系统。
- **增量组织同步对账**：`Syncer.Seed` 只面向全新 store；读现有元组算 deletes 的增量对账未做。
- **知识库/RAG**（P1）：向量库、文档→Markdown 解析层、LangExtract 抽取层都未建。
- **真实客户写操作**：仅完成参考库的 `production_progress` low_write + 人在环确认；任意 SQL/CRUD、高风险写和真实客户系统写入未做。
- **桌面富 UI 待补**：流式输出、Markdown/代码渲染、本地文件 UI（`readFile/writeFile` 主进程已就绪但未接界面）、破坏性操作确认弹窗、应用图标。
- **多 IdP**：适配器接口已抽象，仅 mock 实现。
- **计费**：未实现。

## 8. 技术债（已在代码标注）

- 🔴 **`agentd /chat` 信任请求体 userId**（Phase 0 捷径）→ 生产必须从企微 OAuth 会话/JWT 推导身份，绝不信客户端。已在 `cmd/agentd/main.go` 顶部标注。
- 🟠 桌面"身份下拉"是演示道具，非安全边界（同上）。
- 🟠 `openfga/language` 转换器较重（拉入 gonum/otel），仅加载模型时用；如需瘦身可改内嵌 JSON 模型。
- 🟡 职位自由文本→角色映射靠规则表 + 管理员确认，默认 fail-closed 到 staff。

## 9. 运维/环境备忘

- **git 已初始化** —— 远端 `origin`（GitHub `lingchaojie/merchantAgent`），主干 `main`。secrets 走 gitignored `backend/dev.env`（`dev.env` + `*.env` 双规则），已验证不入 git。
- **本地部署（WSL 后端 + WSL mock + Windows exe）完整步骤见 `docs/本地部署指南.md`**（含中国网 mirror、winCodeSign 绕过、localhost/IPv6 说明、重启流程）。
- OpenFGA 走 docker-compose，**host 端口 18080/18081/13000**（8080 被本机 Sub2API 网关占用、8090/8091 也被占过）。
- WSL 跑 Electron 需 `--disable-gpu --no-sandbox --disable-dev-shm-usage`（真机不需）。
- 真机出 Windows 安装包：`desktop` 里 `electron-builder`（NSIS），在 Windows 侧构建。

## Phase 1 进度（按 `docs/phase1-设计.md` 的里程碑）

| 里程碑 | 内容 | 状态 |
|---|---|---|
| **M1 数据地基** | SQLite 真库 ERP+CRM + 种子 + 两连接器（参数化只读工具） | ✅ 完成 |
| **M2 skill 内核** | skill DB 模型 + OpenFGA `type skill` + guard 改经 skill 中介 + 投影 | ✅ 完成 |
| **M3 云 agentd 编排** | provider 接缝 + Go 工具循环 + 5 层 prompt + **渐进式披露(索引/load_skill)** + 流式 + 会话 + 按租户审计 | ✅ 完成 |
| **M4 客户端/服务端接缝 + 桥** | 桌面改瘦客户端(SSE 流式) + 本地文件反向桥 | ✅ 完成 |
| **M5 一条竖切验证** | 客户360 端到端 + 同问不同权复现 + 实机部署（WSL 后端 + Windows exe） | ✅ 完成（2026-07-12 Windows GUI 核验已补） |
| **M6 管理界面** | CRUD API + 桌面配置页（角色/映射/skill/分配/数据域） | ✅ 完成 |
| **M6.5 Desktop-local tool 竖切** | 签名包 + 参考 SQLite + read/write/read + 审计/撤权/Windows 窄窗 | ✅ 完成 |
| M7 全场景验证 | 4 场景 + 喂本机文件→查库→出文件 | ⬜ |

**M1 交付物**（backend，纯 Go SQLite `modernc.org/sqlite`，免 cgo）：
- `connector/erp/`：内存 SQLite（customers/orders/boms/receivables），种子后置 `PRAGMA query_only` 强制只读；工具 `query_order_status` / `query_order_financials`(cost 域) / `check_material_kitting` / `query_customer_orders`；单测绿（含保住 Phase 0 契约：SO-1001 生产中/profit 18000/螺栓欠 200，及只读拒写）。
- `connector/crm/`：内存 SQLite（contacts/follow_ups/opportunities），同只读纪律；工具 `query_customer_contacts` / `query_customer_followups` / `query_customer_opportunities`；单测绿。
- **跨系统 join 键 = 客户名**（ERP.customers.name ↔ CRM.*.customer_name），为 M5 客户360 铺路。
- `mockerp/`（Phase 0 YAML）暂原样保留，M3 把 e2e 切到 SQLite 后再退役。全量 `go test ./...` 绿、无回归。

**M2 交付物**（skill 内核，已在**真 OpenFGA** 上验证）：
- `skill/`：SQLite 注册表（`templates` + `skills`，可写，为 M6 管理 UI 铺底），种子 1 模板(order-360) + 2 租户 skill(order360/customer360)；`List(tenant)` 读取。
- `skill.Tuples()`：把 skill 投影成 OpenFGA 元组（`skill.usable_by`←角色、`tool.exposed_by`←skill），与 `sync.SnapshotToTuples` 同套路、纯函数、排序幂等。**data_domains 不投影**（仅建议，非授权，守住"能力≠数据"§3.3）。
- `authz/model.fga`：新增 `type skill(usable_by)`；`tool.invoker` 从"直接绑角色"改为 **`usable_by from exposed_by`**——能调工具 = 有一个 usable 的 skill 暴露了它。工具不再直接授予角色。
- `runtime/guard.go`：`Authorize` 首道加 **capability 检查**（`tool#invoker`），在记录级/数据域之前；无 skill 直接拒。
- **验证**（`OPENFGA_API_URL=... go test -count=1 ./...` 全绿，e2e 真跑非 skip）：
  - sales(u_sales1) → 经 sales 角色 usable order360 → 可调订单工具 ✅
  - manager(u_smgr/u_boss) → manager_tier 角色 → 可调 + cost 域 → 看利润 ✅
  - planner(u_plan) → 不在任何 skill → **capability 墙拒** ⛔（新增的第一道闸）
  - `TestGuard_CapabilityWall`（hermetic）：有记录权限但无 skill → 仍拒。
- **里程碑修正**：设计原把"渐进式披露(索引/load_skill)"列在 M2，但它是运行时特性（`load_skill` 元工具依赖 LLM 循环），已并入 M3。

**M3 交付物**（LLM 编排，已用**真 gpt-5.5** 端到端验证）：
- `provider/`：LLM 接缝。`Provider` 接口 + `OpenAI`（OpenAI 兼容 `/v1/chat/completions`，配置 base_url/api_key/model）+ `Fake`（脚本化，hermetic 测试）。模型无关，一行配置切换。
- `runtime/llm.go`：`LLMAgent` 工具调用循环，替换 KeywordRouter。**渐进式披露**：prompt 常驻 skill 索引 + `load_skill` 元工具按需展开剧本并解锁其工具（未加载的工具不进 tools）。**5 层 system prompt**（平台铁律/租户/角色/技能索引/边界告知）。guard 逐次调用；`EventSink` 流式；`Ask` 收发 history（多轮会话）。
- **边界告知（层5）**：`Guard.CanViewDomain` 算出调用者可见的数据域，写进 prompt（中文标签对齐工具描述），模型据此确定性决策，不再靠角色名猜。
- `runtime/audit.go`：加 `Appender` 接口 + `TenantAudit`（**按租户独立哈希链**）。
- `wire/`：组合根。`Resolver`（OpenFGA `ListObjects(usable_by)` ∩ 注册表 → `SkillInfo`，真实能力预过滤）+ `Assemble`（一把装配 store/连接器/registry/resolver/agent + 种子）。
- `cmd/agentd/`：改用 `LLMAgent`；**`/chat` SSE 流式** + 每会话历史（`sessionId`）+ `/audit?tenant=` 按租户；`/login` 不变。secrets 走 gitignored `backend/dev.env`（已验证不入 git）。
- **验证**：
  - hermetic（`Fake` provider）：`TestLLM_ProgressiveDisclosure_HappyPath`（工具在 load_skill 前不出现，之后才出现）、`TestLLM_GuardDeniesWithinLoadedSkill`（skill 内 guard 仍拦 cost）。
  - **真 gpt-5.5**（`wire` live，opt-in）：sales 问进度→自动 load_skill+status+kitting→成话 ✅；sales 问利润→优雅拒、零泄漏、无 allow 财务调用 ✅；manager 问利润→财务 allow、答"利润 18,000/售价 100,000/成本 82,000" ✅。
  - **SSE 实跑**：curl `/chat` 见 `tool_call→skill_loaded→tool_call→tool_result→final→done` 全程流式。
- **重要设计发现**（已解决）：只在 prompt 里"告知边界"不够——模型会从角色名猜、导致该调也不调；改为把"可见数据域"确定性写进 prompt（层5 落地），guard 仍是唯一安全边界（filter-before-grounding 不变）。

**M4 交付物**（桌面瘦客户端 + 反向桥，**M4b 已用真 gpt-5.5 端到端验证**）：
- **M4a 流式瘦客户端**：`shared/contract.ts` 用 `chat(req,onEvent)→Promise<final>` 取代一次性 `ask`；`main/agentd.ts` SSE 客户端（解析 `event:/data:` 帧、跨 chunk 边界的 `\n\n` 分帧）；`ipc.ts`+`preload` 用"每调用一个事件频道 + `sender.send`"把流事件转发给渲染层；`renderer` 用 `foldEvent` 把事件实时折进 pending 消息（工具徽章/状态行/结果卡/最终文本）；浏览器 mock 也改成流式。
- **M4b 本地文件反向桥**（design §2 选项 A）：`connector.FileBridge`（放中立的 connector 包，避免 localfile→runtime 依赖）+ `connector/localfile`（`read_local_file`/`write_local_file` 作为**环境工具**：一律可用、不经 skill、不经 OpenFGA guard——本地文件是用户自己的，自门禁于客户端 fsguard+确认）；`LLMAgent.WithAmbient` 从首轮就提供这些工具；agentd 端 `fileBridge` 发 `file_request` SSE 事件并阻塞在 `pending[reqId]` 通道，`/chat/file-result` 端点唤醒它；桌面 `ipc.ts` 收到 `file_request`→fsguard 读/写（覆盖写弹 `dialog` 确认）→POST 回执。
- `runtime/audit.go`：本地文件操作也进审计（decision=allow, reason="ambient"）。
- **验证**：
  - hermetic：`runtime` `TestLLM_AmbientLocalFile`（环境工具首轮即提供、经 `FileBridge` 往返）；`desktop` `agentd.test.ts`（SSE 解析 + 流循环 + error 事件 + **file_request→onFile→POST 回执** 6 用例）；`fsguard` 5 用例；typecheck 双端 + electron-vite 三端 build 全绿。
  - **真 gpt-5.5 端到端**（`cmd/agentd` `TestBridge_LocalFileRoundTrip`，opt-in）：模型自主链式 `read_local_file(orders.txt)→load_skill→query_order_status(SO-1001)→write_local_file(result.txt)`；`result.txt` 落盘"订单 SO-1001（A公司）当前状态为生产中，承诺交期 2026-07-20"；审计含三工具。反向桥双向往返成立。
- **历史验证缺口（已关闭）**：M4 当时仅有无头验证；2026-07-12 已在 Windows unpacked App 上补齐真实流式渲染、原生写确认、桌面与 390px 窄窗视觉/滚动几何核验。

**M5 交付物**（一条竖切端到端 + 实机部署，2026-07-10）：
- **实机拓扑**：WSL(Ubuntu 24.04) 里 OpenFGA(docker `:18080`) + agentd(Go 进程, 绑 `0.0.0.0:8765`) 分开部署；Windows 侧出 `desktop/dist/merchantAgent Setup 0.1.0.exe`(NSIS 安装包 82MB) + `win-unpacked/merchantAgent.exe`(免安装版)。桌面不 spawn agentd，连已运行的 WSL agentd。LLM 配置在 gitignored `backend/dev.env`（linx2.ai / gpt-5.5）。
- **端到端验证（真 gpt-5.5，经 agentd `/chat` SSE）**：
  - order360：sales 问 SO-1001 → 自主 `load_skill→query_order_status→check_material_kitting` 成话 ✅
  - 同问不同权：sales 问利润→优雅拒零泄漏；manager(u_smgr)/boss 问同一问题→财务 allow（成本 82k/利润 18k/售价 100k）✅
  - customer360 跨系统聚合：boss 问「A公司整体情况」→ `load_skill(customer360)→query_customer_orders(ERP)+query_customer_contacts/followups/opportunities(CRM)` 4 工具跨库、按客户名 join → 拼出客户档案卡（订单+联系人+跟进+20万商机）✅
- **桌面构建修复**（本次落地，已随代码提交）：`127.0.0.1`→`localhost`（本机 WSL2 localhost 转发为纯 IPv6，`::1` 可达、IPv4 字面量不可达；Node fetch autoSelectFamily 命中）；`better-sqlite3` 由直接依赖 `@electron/rebuild` 在打包前用 `-f` 强制切到 Electron ABI，`npmRebuild:false` 仅关闭 electron-builder 冗余且非强制的重建路径。中国网环境构建走 goproxy.cn + npmmirror，并绕过 electron-builder winCodeSign 的 darwin 符号链接权限问题（预解压排除 darwin/）。
- **GUI 核验已补（2026-07-12）**：Windows unpacked App 完成真实流式查询、权限拒绝、原生写确认和跨角色 read-back；桌面/390px 截图与 scroll geometry 均通过。

## 10. M6 管理界面 — 已完成（2026-07-10）

租户管理员自助配置落地：**建角色 / 改职位映射 / 编 skill / 分配（两个闸）/ 授数据域**，改动落盘持久化并实时投影进 OpenFGA。

- **新增 `config/` 配置库**（文件型 SQLite 真源：roles/role_rules/data_domains/domain_grants）+ `config.Tuples` 纯投影 + `config.LoadRules`。skill 库补齐 CRUD/CloneTemplate/OpenFile。
- **`wire.Projector` 投影引擎**（收口成一条主轴，偿还 `authz.Syncer.Seed` 的 P0 增量对账技术债）：全量重算期望集 → `authz.ReadTuples` 读当前 → `sync.Reconcile`（含删除）→ `ApplyDiff`。启动投影 + 每次 admin 写都重投影。Projector 是 OpenFGA 唯一写者。
- **`/admin/*` REST CRUD**（`requireAdmin` 每端点先 `Check(user,admin,tenant)`，非 admin→403；`X-User-Id` 由桌面注入）。grant subject 格式校验（防坏值把重投影卡死→重启 log.Fatalf 变砖）。
- **桌面配置页**：generic `admin` IPC 通道（注入 X-User-Id）+ typed `makeAdminClient` + `AdminView` + 5 个 Pane（角色/职位映射/技能/分配/数据域）+ 齿轮入口视图切换。角色支持新建/行内编辑/删除；职位规则可上下排序；skill 支持空白新建/模板克隆/全字段编辑/删除；非管理员配置入口禁用，后端鉴权仍是最终边界。
- **浏览器 mock**：改为与真实 API 同形的会话内存真源，包含真实 seed 角色/规则/skill/模板/数据域，使用合法 OpenFGA subject，并支持完整 CRUD，浏览器验收可重复执行。
- **实机与自动化验收（真跑，非声称）**：管理员经真实 HTTP admin API 给 order360 加 planner→OpenFGA 里 `role:.../planner#assignee usable_by order360` 元组即时出现（闸 A 实时开）；撤下→差量对账删除该元组（**撤权实时**）；非 admin PUT→403；config.db+skills.db 落盘。后端全量测试套件全绿；桌面 Vitest **32/32**、双端 typecheck、Electron 三端生产构建全绿。浏览器完成角色/规则/skill/两道分配闸/数据域 CRUD 验收，桌面与 390px 窄窗口均无横向溢出，页面控制台 0 error。

**遗留（超出 M6 范围）**：tag 派生角色不在 UI 管（仍自动投影）；reproject 与两个 DB 写非事务（reproject 失败会使 DB 领先 OpenFGA 到下次写/重启）。

## 11. Desktop-local enterprise tool 竖切 — 已完成（2026-07-12）

- **确定性后端**：`provider.Fake` 驱动真实 `/chat` SSE，planner 完成 60% read → confirmed 80% write → 80% read；请求断言 package/version/digest、role/device/idempotency，审计链断言 before/after。sales 强制尝试写时无 `local_tool_request` 且审计 `deny/denied`。
- **确定性桌面**：真实签名参考包 + temp SQLite 完成 read/write/read、重复幂等键不增版本、取消不变更、tampered version/signature 在 store 前拒绝。冲突、unknown/timeout 等终态由相邻 executor/bridge 测试覆盖。
- **发布矩阵**：backend 两轮 `go test -count=1 ./...`（第二轮显式 OpenFGA）全绿；desktop Vitest 102/102、typecheck、build、`dist:dir` 全绿；产物含 capability、公钥和可被 Electron 加载的 x64 `better_sqlite3.node`。
- **Windows 真链**：OpenFGA + 当前分支 WSL agentd + Windows unpacked App + gpt-5.5 跑通 60→80；原生对话框显示 SO/WO/before/after/note 后确认；tenant audit `verified=true` 且 write 带 confirmed/before/after；Assign pane 撤掉 planner 后新会话立即拒写。
- **Gate A/Gate B 真机探针**：临时把 `production-progress` 从仅 `manager_tier` 暴露为 `manager_tier + sales`，使 `u_sales1` 的真实模型写调用通过 Gate A；Gate B 随后以 `business_record#operator` 拒绝 `SO-1001`，没有 `local_tool_request`、原生确认或 SQLite 修改。`/audit` 返回 `verified=true`，真实条目为 `decision=deny,status=denied,confirmed=false,reason="no operator access to business_record order/SO-1001"`。探针后立即恢复为仅 `manager_tier`。
- **诚实边界**：正常配置下，真实模型可能因 sales 无写 Skill 而直接文本拒绝，不产生工具调用或 tool-deny；上面的临时暴露探针专门区分并实证了 Gate B。参考 SQLite、mock userId 和种子订单不是客户集成或生产身份。

## 12. 建议的下一步（任选）

1. **接企微真登录**：实现 `WeComAdapter` + OAuth（需先解决备案域名）；把 `X-User-Id` demo 注入换成验证过的会话派生。
2. **桌面富 UI 续做**：Markdown 渲染、本地文件接入 UI、应用图标。
3. **进 P1**：权限感知 RAG（向量库 + Markdown 解析 + LangExtract）。
